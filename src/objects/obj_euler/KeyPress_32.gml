timer = get_timer();

/*
var filesArray = files_list(working_directory, "txt");
var len = array_length(filesArray);

var file, line;

for (var i = 0; i < len; i++) {
	
	file = file_text_open_read(filesArray[i]);
	while (!file_text_eof(file)) {
		
		line = file_text_read_string(file);
		
		
		
		
		
		file_text_readln(file);
	}
	
	file_text_close(file);
}
*/




reset_timer  = true;